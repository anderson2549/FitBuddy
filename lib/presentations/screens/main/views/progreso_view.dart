import 'package:flutter/material.dart';

class ProgresoView extends StatelessWidget {
  const ProgresoView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Progreso'));
  }
}
